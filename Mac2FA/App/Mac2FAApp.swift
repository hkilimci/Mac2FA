import SwiftUI

@main
struct Mac2FAApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.automatic)
        .defaultSize(width: 600, height: 500)
    }
}
